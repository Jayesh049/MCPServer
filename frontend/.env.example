# Copy to .env.local — URL of the MCPServer HTTP API (same host/port as `npm run dev` in repo root).
# Default Node port is 3333; use 127.0.0.1 on Windows to avoid proxy quirks.
MCP_API_BASE_URL=http://127.0.0.1:3333
