# Dependencies
node_modules/

# Next.js
.next/
out/

# Build output (rebuild with npm run build)
dist/
dist.zip
*.zip

# Env and secrets (local SQLite path may be checked in optionally)
.env
prisma/rag.sqlite
prisma/rag.sqlite-journal
prisma/**/*.sqlite
prisma/**/*.sqlite-journal
.env.*
!.env.example

# Frontend local overrides (API base URL); do not commit machine-specific URLs
frontend/.env.local

# Logs and caches
*.log
npm-debug.log*
.turbo/
.cache/

# OS / editor
.DS_Store
Thumbs.db
.cursor/

# Local scratch copies (duplicates; real sources live under src/)
files (1)/
files (2)/

# Python ML generated artifacts (see ml/README.md)
ml/artifacts/
**/__pycache__/
*.py[cod]
ml/scripts/__pycache__/
ml/flask_disease/**/__pycache__/

# Optional JSON export from DB (see ml/scripts/export_diseases_to_json.py)
ml/diseases/*.json

# Flask disease ML local artifacts (also under ml/artifacts/)
ml/artifacts/disease_ml/
